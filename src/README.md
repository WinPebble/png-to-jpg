# Source

The exact original `v1.0.0` scripts have not yet been verified against the
published release package, so reconstructed code is intentionally not stored
here as historical source.

See `../SOURCE_STATUS.md`.
